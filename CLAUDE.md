# agent_skratch_pad

This repository is a scratch working directory for the Cyrus agent. It exists only to satisfy Cyrus's requirement that every session run inside a git repository — it is not a real project.

## IMPORTANT

Do not write, edit, or commit any code unless explicitly asked to in the issue or the conversation. Default to responding, researching, planning, or answering directly instead of producing code changes.
